/*
Step 1: Define a variable called myColor that has an array of colors as its value.
Step 2: Define a variable called arrayToString and join the elements of the array into a string with a space
in between each element, using the join method.
Step 3: Log into the console the arrayToString variable.
*/

const myColor = ["Red", "Green", "White", "Black"];
const arrayToString = myColor.join(" ");
console.log(arrayToString);